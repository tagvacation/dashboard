# GCS Service Account (Account B) — paste the full JSON as ONE SINGLE LINE
# Get it from: GCP Console → IAM → Service Accounts → vertex-express@gen-lang-client-0866402603 → Keys → Create JSON
# Then minify: cat key.json | tr -d '\n' | sed 's/"/\\"/g'  ← NO, just paste the raw JSON as one line
GCS_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"gen-lang-client-0866402603",...}

# GCS Bucket
GCS_BUCKET=ai_clip_007

# Google Sheet
SHEET_ID=11m9qc_j43JtIN1EnRrCw1nK6AZ9y4Snn6DoWWUiNkMw
SHEET_TAB=Sheet2

# Dashboard auth
DASHBOARD_PASSWORD=choose_a_strong_password
